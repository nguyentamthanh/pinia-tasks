<template>
  <div class="task">
    <h3>{{ task.title }}</h3>
    <div class="icons">
      <div>{{ taskScore }}</div>
      <i class="material-icons" @click="taskStore.deleteTask(task.id)"
        >delete</i
      >
      <i class="material-icons" @click="taskScore.toggleFav(task.id)"
        >favorite</i
      >
    </div>
  </div>
</template>

<script>
import { useTaskStore } from "../stores/TaskStore";

export default {
  props: ["task"],
  setup() {
    const taskScore = useTaskStore();
    return { taskScore };
  },
};
</script>

<style></style>
